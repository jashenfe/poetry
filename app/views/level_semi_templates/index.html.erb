<div class="row mb-3">
  <div class="col-md-12">
    <h1>
      All level semis
    </h1>

    <a href="/level_semis/new" class="btn btn-block btn-outline-secondary">
      Add a new level semi
    </a>
  </div>
</div>

<!-- Ransack Code start -->

<!-- Ransack Code end -->

<!-- Location Map Code start -->

<!-- Location Map Code end -->

<hr>

<div class="row mb-3">
  <div class="col-md-12">
    <table class="table">
      <tr>
        <th>
          ID
        </th>

        <th>
          Created at
        </th>

        <th>
          Updated at
        </th>

        <th>
        </th>
      </tr>

      <% @level_semis.each do |level_semi| %>
      <tr>
        <!-- Display level_semi.id start -->
        <td>
          <%= level_semi.id %>
        </td>
        <!-- Display level_semi.id end -->

        <!-- Display level_semi.created_at start -->
        <td>
          <%= time_ago_in_words(level_semi.created_at) %> ago
        </td>
        <!-- Display level_semi.created_at end -->

        <!-- Display level_semi.updated_at start -->
        <td>
          <%= time_ago_in_words(level_semi.updated_at) %> ago
        </td>
        <!-- Display level_semi.updated_at end -->

        <td>
          <a href="/level_semis/<%= level_semi.id %>">
            Show details
          </a>
        </td>
      </tr>
      <% end %>
    </table>

    <div class="row mb-3">
      <div class="col-md-12">
        <%= paginate @level_semis, theme: "twitter-bootstrap-4" %>
      </div>
    </div>

  </div>
</div>
