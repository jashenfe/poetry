<div class="row mb-3">
  <div class="col-md-8 offset-md-2">
    <h1>
      Coach #<%= @coach.id %> details
    </h1>

    <div class="row mb-3">
      <div class="col">
        <a href="/coaches" class="btn btn-block btn-outline-secondary">
          Go back
        </a>
      </div>

      <div class="col">
        <a href="/coaches/<%= @coach.id %>/edit" class="btn btn-block btn-outline-secondary">
          Edit coach
        </a>
      </div>

      <div class="col">
        <a href="/delete_coach/<%= @coach.id %>" class="btn btn-block btn-outline-secondary">
          Delete coach
        </a>
      </div>
    </div>

    <dl>
      <dt>
        First name
      </dt>
      <dd>
        <%= @coach.first_name %>
      </dd>

      <dt>
        Last name
      </dt>
      <dd>
        <%= @coach.last_name %>
      </dd>

      <dt>
        Team
      </dt>
      <dd>
        <% if @coach.team.present? %>
          <a href="/teams/<%= @coach.team_id %>">
            <%= @coach.team.bout_id %>
          </a>
        <% end %>
      </dd>

      <dt>
        Created at
      </dt>
      <dd>
        <%= time_ago_in_words(@coach.created_at) %> ago
      </dd>

      <dt>
        Updated at
      </dt>
      <dd>
        <%= time_ago_in_words(@coach.updated_at) %> ago
      </dd>
    </dl>
  </div>
</div>

